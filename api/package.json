{
  "name": "@teapi-poc/api",
  "version": "0.1.0",
  "description": "Typescript Express API - Proof of concept (Actual API Server)",
  "main": "index.ts",
  "scripts": {
    "buildDeps": "cd ../data && pnpm run build",
    "start": "pnpm run buildDeps && ts-node src/index.ts",
    "build": "pnpm run buildDeps && tsc",
    "serve": "node dist/index.js"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "express": "^4.21.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@teapi-poc/data": "workspace:^"
  }
}
